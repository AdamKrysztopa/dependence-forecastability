"""Diagnostics estimator-math relocated under the services layer (v0.5.0).

Intentionally import-light: submodules are reached via explicit import paths
(``forecastability.services.diagnostics.<module>``) so that relocating this
package does not introduce a runtime circular import through
``forecastability.services``.
"""
