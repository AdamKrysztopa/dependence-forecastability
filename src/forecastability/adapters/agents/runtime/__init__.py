"""Agent runtime adapters — PydanticAI agent definitions, runners, and dep containers.

This subpackage contains the live agent implementations for every LLM-backed
workflow: triage, fingerprint, screening, covariant interpretation, and
routing-validation narration.
"""
